# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository currently contains no source code — only a `README.md`. There
is no build system, package manifest, linter, or test suite configured yet.
Do not assume the presence of any particular language, framework, or tooling
until it is actually added to the repo; check for config files (e.g.
`package.json`, `pyproject.toml`, `go.mod`, etc.) before relying on commands
from prior sessions or general conventions.

## Project purpose

From `README.md` (Polish):

> polonia
> organizacje wspierające polonię, mające swoją siedzibę w Polsce

Translation: a project about organizations that support the "Polonia" (the
Polish diaspora) which are based in Poland.

## Working here

Since the repo is effectively a blank slate, when adding the first code:
- Establish the language/framework based on explicit user direction, not assumption.
- Update this file with real build/lint/test commands and architecture notes once they exist, rather than leaving generic placeholders.
